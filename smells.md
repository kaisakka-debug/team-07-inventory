ตัวแปรชื่อตั้งแบบมักง่าย (Poor Naming): p, q, t, m, tot ไม่สื่อความหมายเลย ต้องมานั่งเดาว่า p คือ price, q คือ quantity, t คือ type ของลูกค้าใช่หรือไม่
ตัวเลขเวทมนตร์ (Magic Numbers): มีตัวเลขลอยๆ โผล่มาเต็มไปหมด เช่น 1000, 2, 0.15, 50, 0.07 โดยไม่มีการประกาศเป็นค่าคงที่ (Constant) ที่สื่อความหมาย
เงื่อนไขซ้อนทับกันลึกเกินไป (Deep Nesting): มีการใช้ if-else ซ้อนกันถึง 3 ชั้น ทำให้ติดตาม Logic ว่าตกไปอยู่เงื่อนไขไหนได้ยากมาก
ตรรกะแอบแฝงที่คาดไม่ถึง (Hidden Side-effects): การเช็ค int(tot) % 10 == 0 แล้วลบ 1 เป็นตรรกะที่เดาไม่ได้จากชื่อฟังก์ชันและดูไม่สมเหตุสมผลทางธุรกิจ
ขาดการกำหนดชนิดข้อมูล (Missing Type Hints): ฟังก์ชันไม่มีการบอกว่า parameter แต่ละตัวรับค่าเป็น int, float หรือ bool และไม่มี Return Type