# SOLID Design Review

| หลัก SOLID | ละเมิดหรือไม่ | จุดที่เกี่ยวข้อง (class/method) | อธิบาย/ผลกระทบ | ข้อเสนอปรับปรุง |
| :--- | :--- | :--- | :--- | :--- |
| **S (SRP)** | [x] ละเมิด | `NotificationService` / `main.py` | คลาสหรือฟังค์ชันรับหน้าที่เยอะเกินไป ทั้งคำนวณประมวลผล แสดงผลลัพธ์ (UI) และทำการบันทึก Log ในจุดเดียว | แยกความรับผิดชอบ เช่น แยกส่วนคำนวณ (Business Logic) ออกจากส่วนแสดงผล (UI) และ Logger |
| **O (OCP)** | [x] ละเมิด | `service.py` (`send_notification`) | มีการใช้เงื่อนไข `if channel == "email": ... elif channel == "sms": ...` หากเพิ่มช่องทางใหม่ต้องแก้โค้ดเดิม | ใช้ Polymorphism / Interface ร่วมกับ Abstract Class เพื่อให้รองรับช่องทางใหม่โดยไม่ต้องแก้ไขโค้ดเดิม |
| **L (LSP)** | [ ] ไม่ละเมิด | - | คลาสย่อยยังคงปฏิบัติตามสัญญาการทำงานของคลาสแม่ได้ตามปกติ ไม่พบพฤติกรรมผิดเพี้ยน | รักษาโครงสร้างเดิมไว้ |
| **I (ISP)** | [ ] ไม่ละเมิด | - | Interface หรือ Method ที่ใช้อยู่ยังไม่มีขนาดใหญ่เกินไปที่บังคับให้คลาสอื่นต้อง Implement สิ่งที่ไม่จำเป็น | แยก Interface ให้กระชับตามหน้าที่เฉพาะเมื่อมีการขยายระบบ |
| **D (DIP)** | [x] ละเมิด | `service.py` | เรียกใช้และสร้าง Instance ของ `EmailNotifier` หรือคลาสอื่นๆ โดยตรง (Hard-coded dependency) | ใช้ Dependency Injection (DI) ส่ง Notifier Instance เข้ามาผ่าน Constructor หรือ Method แทน |
